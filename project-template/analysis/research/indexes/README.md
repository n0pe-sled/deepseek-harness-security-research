# Analysis Indexes

Place derived, snapshot-bound indexes here. An index is disposable and never
overrides the baseline artifact/hash manifest. Include its snapshot ID in the
filename when more than one snapshot is retained.
